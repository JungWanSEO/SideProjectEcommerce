# 대시보드 자동 로드 — /var/lib/grafana/dashboards 의 JSON을 기동 시 등록한다.
apiVersion: 1
providers:
  - name: commerce
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
